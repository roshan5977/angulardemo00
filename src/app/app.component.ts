import { Component } from "@angular/core";

@Component({
    selector: "app",
    template: "<router-outlet></router-outlet>"
    // it only have the work is what ever comes that what ever comes from router tree it will be in router-outlet nothing it in his hand ..
})

export class AppComponent { }


